---
title: f5xc-firecrawl
description: >-
  Lokales, selbst gehostetes Web-Scraping und Crawling über die
  Open-Source-Engine Firecrawl.
sidebar:
  order: 13
i18n:
  sourceHash: a9d67be9cf3e
  translator: machine
---

import { Aside, Badge } from '@astrojs/starlight/components';

Das Plugin **f5xc-firecrawl** bietet lokales, selbst gehostetes Web-Scraping
über die Open-Source-Engine [firecrawl](https://github.com/mendableai/firecrawl).
Keine API-Schlüssel, keine Abonnements, keine Cloud-Abhängigkeit. Alle
Operationen werden gegen die lokale Firecrawl-Instanz auf `localhost:3002`
innerhalb des Devcontainers ausgeführt.

<Badge text="v1.1.0" variant="note" /> <Badge text="Productivity" variant="tip" />

## Installation

```
/plugin install f5xc-firecrawl@f5xc-salesdemos-marketplace
```

## Befehle

### /scrape

Eine einzelne URL scrapen und Inhalte als Markdown extrahieren.

```
/scrape https://docs.example.com/getting-started
/scrape https://example.com --format markdown,links --wait 2000
```

### /batch-scrape

Mehrere URLs gleichzeitig scrapen.

```
/batch-scrape https://example.com https://example.org https://example.net
```

### /crawl

Mehrere Seiten ausgehend von einer Start-URL crawlen.

```
/crawl https://docs.example.com --limit 20 --depth 2
/crawl https://docs.example.com --include /api/* --exclude /blog/*
```

### /map

Alle URLs einer Website entdecken.

```
/map https://docs.example.com
/map https://docs.example.com --search api --subdomains
```

### /search

Im Web suchen und optional Ergebnisse scrapen.

```
/search "firecrawl web scraping" --limit 10
/search "AI tools 2026" --scrape --time month
```

### /extract

LLM-gestützte strukturierte Datenextraktion aus Webseiten.

```
/extract https://example.com "Extract the main heading and any links"
/extract https://example.com/pricing --schema '{"plans": [{"name": "string", "price": "string"}]}'
```

<Aside type="note">
  Der extract-Befehl erfordert einen OpenAI-kompatiblen LLM-Proxy,
  der über `OPENAI_BASE_URL` und `OPENAI_API_KEY` konfiguriert ist.
</Aside>

### /llmstxt

Eine llms.txt-Datei für eine Website generieren.

```
/llmstxt https://docs.example.com
```

## Skills

### web-scraper

Wird automatisch aktiviert, wenn Sie eine URL scrapen, eine Website crawlen,
Website-URLs kartieren, im Web suchen, strukturierte Daten extrahieren,
llms.txt generieren, mehrere URLs per Batch scrapen oder eine Webseite
in Markdown konvertieren möchten. Delegiert sofort an den firecrawl-operator-Agenten.

## Agenten

### firecrawl-operator

Autonomer Web-Scraping-Agent, der `curl` + `jq`-Sequenzen
gegen die lokale Firecrawl-API ausführt. Unterstützt 11 Protokolle, die alle
v1-Endpunkte abdecken. Schreibgeschützter Agent (keine Write-, Edit- oder Agent-Tools).

| Protokoll | Endpunkt | Typ |
|----------|----------|------|
| HEALTH | `GET /` | Sync |
| SCRAPE | `POST /v1/scrape` | Sync |
| BATCH_SCRAPE | `POST /v1/batch/scrape` | Async |
| CRAWL | `POST /v1/crawl` | Async |
| CRAWL_CANCEL | `DELETE /v1/crawl/:id` | Sync |
| CRAWL_ACTIVE | `GET /v1/crawl/active` | Sync |
| CRAWL_ERRORS | `GET /v1/crawl/:id/errors` | Sync |
| MAP | `POST /v1/map` | Sync |
| SEARCH | `POST /v1/search` | Sync |
| EXTRACT | `POST /v1/extract` | Async |
| LLMSTXT | `POST /v1/llmstxt` | Async |

## Infrastruktur

Das Plugin erfordert den Firecrawl-Stack, der im Devcontainer läuft:

| Komponente | Port | Zweck |
|-----------|------|---------|
| Firecrawl API | 3002 | Alle Scrape/Crawl/Map/Search/Extract-Endpunkte |
| Playwright | 3000 | JavaScript-Rendering-Engine |
| Redis | 6379 | Job-Queue-Backend |
| PostgreSQL | Socket | Persistenz für Crawl-/Batch-Jobs |
| LiteLLM-Proxy | OPENAI_BASE_URL | LLM-Backend für Extract (optional) |

Der Stack startet automatisch, wenn `ENABLE_FIRECRAWL=true` gesetzt ist (die
Standardeinstellung). Ein SessionStart-Hook prüft, ob die API erreichbar ist,
und warnt, wenn der Dienst nicht verfügbar ist.

## Unterschiede zu Cloud-Firecrawl

Dieses Plugin verwendet die selbst gehostete Open-Source-Version:

- Keine Authentifizierung oder API-Schlüssel für das Scraping erforderlich
- Keine Kreditlimits oder Ratenbegrenzung
- Verwendet v1-API-Endpunkte (nicht v2)
- Browser-Sessions und Deep Research nicht verfügbar
- Extract verwendet Ihren eigenen LLM-Proxy anstelle gehosteter Modelle
- Läuft vollständig innerhalb des lokalen Container-Netzwerks
