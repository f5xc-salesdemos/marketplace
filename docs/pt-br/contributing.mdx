---
title: Contribuindo com um Plugin
description: >-
  Guia para criar, estruturar e publicar plugins para o marketplace
  f5xc-salesdemos.
sidebar:
  order: 6
i18n:
  sourceHash: 36a56024cade
  translator: machine
---

import { Steps, Aside, FileTree } from '@astrojs/starlight/components';

Este guia explica como criar um novo plugin e adicioná-lo
ao marketplace f5xc-salesdemos.

## Estrutura do Plugin

Cada plugin reside em seu próprio diretório dentro de `plugins/` e
segue esta estrutura:

<FileTree>
- plugins/
  - my-plugin/
    - .claude-plugin/
      - plugin.json
    - skills/
      - my-skill/
        - SKILL.md
        - references/
          - reference-data.md
    - commands/
      - my-command.md
    - agents/
      - my-agent.md
    - README.md
</FileTree>

## Criando um Plugin

<Steps>

1. **Crie o diretório do plugin**

   ```bash
   mkdir -p plugins/my-plugin/.claude-plugin
   mkdir -p plugins/my-plugin/skills
   mkdir -p plugins/my-plugin/commands
   ```

2. **Escreva o `plugin.json`**

   Crie `plugins/my-plugin/.claude-plugin/plugin.json`:

   ```json
   {
     "name": "my-plugin",
     "description": "What this plugin does",
     "version": "1.0.0",
     "author": {
       "name": "f5xc-salesdemos"
     },
     "homepage": "https://github.com/f5xc-salesdemos/marketplace/tree/main/plugins/my-plugin",
     "keywords": ["relevant", "keywords"],
     "license": "Apache-2.0",
     "repository": "https://github.com/f5xc-salesdemos/marketplace"
   }
   ```

3. **Adicione skills**

   Crie `plugins/my-plugin/skills/my-skill/SKILL.md` com
   frontmatter:

   ```markdown
   ---
   name: my-skill
   description: One-line description of what this skill does
   ---

   Detailed instructions for Claude when this skill activates.
   ```

   Coloque quaisquer dados de referência em um subdiretório `references/`
   ao lado do `SKILL.md`.

4. **Adicione comandos**

   Crie `plugins/my-plugin/commands/my-command.md`:

   ```markdown
   ---
   description: What this command does
   argument_hint: "[optional-args]"
   allowed_tools:
     - Read
     - Glob
     - Grep
   ---

   Instructions for Claude when the user invokes this command.
   ```

5. **Adicione o plugin ao marketplace.json**

   Adicione uma entrada ao array `plugins` em
   `.claude-plugin/marketplace.json`:

   ```json
   {
     "name": "my-plugin",
     "description": "What this plugin does",
     "version": "1.0.0",
     "author": { "name": "f5xc-salesdemos" },
     "source": "./plugins/my-plugin",
     "category": "productivity",
     "homepage": "https://github.com/f5xc-salesdemos/marketplace/tree/main/plugins/my-plugin",
     "license": "Apache-2.0",
     "keywords": ["relevant", "keywords"],
     "tags": ["searchable", "tags"],
     "repository": "https://github.com/f5xc-salesdemos/marketplace"
   }
   ```

6. **Teste localmente**

   Teste seu plugin sem publicá-lo apontando o Claude Code
   para o diretório local:

   ```bash
   claude --plugin-dir ./plugins/my-plugin
   ```

   Verifique se as skills são ativadas no contexto correto e se
   os comandos produzem a saída esperada.

</Steps>

## Enviando Seu Plugin

<Aside type="tip" title="Fluxo de trabalho do repositório">
  Este repositório aplica um fluxo de governança rigoroso.
  Consulte [CONTRIBUTING.md](https://github.com/f5xc-salesdemos/marketplace/blob/main/CONTRIBUTING.md)
  para o processo completo.
</Aside>

<Steps>

1. Crie uma issue no GitHub descrevendo o novo plugin
2. Crie um branch de feature a partir de `main`
3. Adicione o diretório do seu plugin e atualize o `marketplace.json`
4. Adicione uma página de documentação em `docs/plugins/my-plugin.mdx`
5. Abra um PR vinculado à issue com `Closes #N`
6. Corrija quaisquer falhas de CI e faça o merge após as verificações passarem

</Steps>

## Boas Práticas

- **Mantenha as skills focadas** — cada skill deve fazer uma coisa bem feita
- **Use arquivos de referência** — coloque grandes conjuntos de dados, schemas e
  tabelas de consulta em `references/` em vez de inline no SKILL.md
- **Defina o escopo de forma inteligente** — verifique alterações não commitadas para
  reduzir o conjunto de trabalho quando possível
- **Agrupe a saída por severidade** — use os níveis ERROR, WARNING e INFO
  para que os usuários possam priorizar as correções
- **Escreva um README** — inclua exemplos de uso e uma descrição
  do que cada skill e comando faz
