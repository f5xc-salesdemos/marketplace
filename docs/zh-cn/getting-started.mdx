---
title: 快速入门
description: 添加 f5-sales-demo 市场并安装第一个插件的分步指南。
sidebar:
  order: 2
i18n:
  sourceHash: 8b7375e24be2
  translator: machine
---

import { Steps, Aside, Code } from '@astrojs/starlight/components';

本指南将引导您完成将 f5-sales-demo
市场添加到 Claude Code 并安装第一个插件的操作。

## 前提条件

- Claude Code **v1.0.33** 或更高版本（支持插件市场功能）
- 拥有
  [f5-sales-demo](https://github.com/f5-sales-demo)
  组织访问权限的 GitHub 账户

## 安装

<Steps>

1. **添加市场**

   在任意 Claude Code 会话中，运行：

   ```
   /plugin marketplace add f5-sales-demo/marketplace
   ```

   这将注册该市场为插件来源。此操作只需执行一次。

2. **浏览可用插件**

   列出市场提供的插件：

   ```
   /plugin marketplace list f5-sales-demo-marketplace
   ```

3. **安装插件**

   安装您需要的插件：

   ```
   /plugin install f5xc-docs-tools@f5-sales-demo-marketplace
   /plugin install f5xc-sales-engineer@f5-sales-demo-marketplace
   ```

4. **使用插件**

   导航到任意 f5-sales-demo 内容仓库。

   对于 **docs tools**，运行 MDX 审查命令：

   ```
   /f5xc-docs-tools:review-mdx
   ```

   对于 **sales engineer**，使用触发短语，如
   "prepare the demo"、"run the demo" 或 "walk through
   the demo" — 插件会自动激活。

</Steps>

## 更新插件

将已安装的插件更新到最新版本：

```
/plugin update f5xc-docs-tools@f5-sales-demo-marketplace
/plugin update f5xc-sales-engineer@f5-sales-demo-marketplace
```

## 卸载

移除插件：

```
/plugin uninstall f5xc-docs-tools@f5-sales-demo-marketplace
/plugin uninstall f5xc-sales-engineer@f5-sales-demo-marketplace
```

完全移除市场：

```
/plugin marketplace remove f5-sales-demo-marketplace
```

## 团队配置

要为整个团队预配置市场，请将其添加到项目的
`.claude/settings.json` 文件中：

```json
{
  "extraKnownMarketplaces": [
    "f5-sales-demo/marketplace"
  ],
  "enabledPlugins": [
    "f5xc-docs-tools@f5-sales-demo-marketplace",
    "f5xc-sales-engineer@f5-sales-demo-marketplace"
  ]
}
```

<Aside type="tip" title="共享设置">
  将 `.claude/settings.json` 提交到仓库后，
  每位团队成员都会自动获取市场和插件配置
  — 无需手动设置。
</Aside>

这确保了每位贡献者从第一次在仓库中使用 Claude Code
会话时就拥有相同的工具集。
