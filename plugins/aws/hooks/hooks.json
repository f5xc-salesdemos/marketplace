{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v aws > /dev/null 2>&1 || echo 'WARNING: AWS CLI (aws) is not installed. Run /aws:setup to install and configure.'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
